name: CI

on:
  push:
    branches: [ main ]

jobs:
  linting:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        version: ["3.10"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.version }}

      - name: Show OS and Python
        run: |
          echo "OS: ${{ matrix.os }}"
          echo "Python: ${{ matrix.version }}"
          python --version

      - name: Install flake8
        run: pip install flake8

      - name: Run flake8
        run: flake8 .